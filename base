/** メモ **/
'use strict'
/**  */
var log = console.log;
var splitN = (N) => N.split().split(' ').map(Number);
function Main(input){
	
}
// Main(require("fs").readFileSync("/dev/stdin", "utf8").trim());
